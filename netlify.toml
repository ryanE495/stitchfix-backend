[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so React Router routes (e.g. /customers/abc) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
